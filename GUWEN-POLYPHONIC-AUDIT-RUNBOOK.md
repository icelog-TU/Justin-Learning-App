# 古文全篇多音字實聽 Runbook

> 目的：讓每一篇古文對話只用兩句短指令，就能完成「全篇掃描 → 正式批次建檔 → 使用者實聽 → 中央讀回 → 增刪讀音提示」。
>
> 本檔只負責操作順序與對話交接。資料 schema、有效性判定、提示格式、故障排除及驗收細節仍以 `GUWEN-TTS-CENTRAL-DATABASE-HANDOFF.md`、`GUWEN-WORKFLOW-SOP.md` 為準；不得把本檔解讀成第二套資料庫或第二套技術規格。

## 一、所有 agent 都必須辨識的兩句指令

第一階段觸發句：

```text
第 N 篇已經定稿。請執行正式多音字實聽流程第一階段。
```

第二階段觸發句：

```text
第 N 篇已經全部實聽完成。請讀回中央結果並套用讀音加註。
```

意思相同的自然語句也算觸發，例如「這一篇寫完了，送多音字實聽」或「我全部聽完了，請讀回結果」。不得要求使用者重新貼上長版流程。

收到任一觸發句後，agent 必須先完整閱讀：

- `AGENTS.md`
- `FULL-SITE-HANDOFF.md`
- `GUWEN-WORKFLOW-SOP.md`
- `GUWEN-PROJECT-STATUS.md`
- `GUWEN-TTS-CENTRAL-DATABASE-HANDOFF.md`
- 該篇教材主檔
- `.claude/skills/guwen-decoder/SKILL.md`
- `.claude/skills/design-guwen-decoding/SKILL.md`
- skill 指定的相關 reference

固定使用分支 `claude/chinese-learning-app-justin-yjcfam`、既有 catalog、既有中央資料庫及正式實聽台：

<https://icelog-tu.github.io/Justin-Learning-App/#/tts-audit>

## 二、篇章識別與隔離

每篇必須有三個可核對的識別值：

```text
篇次：N
lessonId：repository 內該篇既有的穩定 lessonId
篇名：《完整篇名》
```

1. 優先從教材主檔與 App 正式資料讀取既有 `lessonId`，不得只靠聊天記憶猜測或另創近似 ID。
2. 開始時先回報解析到的篇次、`lessonId`、篇名與教材主檔。
3. 只有遇到同篇次對應多個現行主檔、找不到穩定 `lessonId`，或主檔與 App 資料互相衝突時，才向使用者提出一個最短的阻塞問題。
4. catalog、中央讀回與教材修改都必須用同一 `lessonId`；不得讀取或套用其他篇章結果。
5. 同一時間原則上只讓一個對話修改同一篇主檔。不同篇可並行，但修改共用 catalog／狀態檔前後都要重新同步遠端。

## 三、第一階段：定稿後建立正式實聽批次

### 3.1 就緒檢查

「已定稿」是指本次列入實聽的播放白名單文字已完成。白名單固定只有：

- 任務開場與本篇原文播放。
- App 引導語。
- 古文線索及其破解白話；出處不朗讀。
- 任務／題目、選項、答對回饋、答錯提示。

詳解、成人編輯資料、來源／出處、學習目標、證據邊界、故事畫面、排序結果、白話卷軸、完成畫面、徽章訊息及其他只顯示不播放的文字，一律不進入多音字實聽 catalog。若 App 日後真的新增朗讀，必須先修改白名單與 Runbook，再另行送測，不得因文字出現在孩子畫面就自行推定會播放。

若仍有待審文字，或 exact playable utterance 尚未確定：

- 明確列出尚未完成之處。
- 不得把不完整批次宣稱為全篇最終實聽。
- 可以保留逐題候選盤點，但須等全文完成後重新做獨立全篇掃描。

若使用者採用「教材主檔先定稿並完成實聽，最後才放入 App」的流程，已核准主檔就是本階段的語音規格來源，不必先實作 App。只從上述白名單建立 exact playable utterance；未來 App 實作必須沿用同一文字、語音單元邊界、`ttsSafe()` 輸入及 voice 條件，若有任何變更則依第六節使舊結果失效並重測。

### 3.2 Agent 必須完成的工作

1. 更新固定分支並執行 `npm run tts:audit:pull`，先讀中央現況。
2. 逐一列出播放白名單內的 exact playable utterance；不只掃古文原句，也不得納入白名單外的詳解或成人文字。
3. 對白名單語音單元找出全部多音字，不得依 agent 猜測「容易念錯」與否而篩除。
4. 先合併文字完全相同的語音單元，再以「字形＋指定讀音＋會影響讀音的詞義／詞組用法＋相同 `ttsSafe()`／voice 條件」建立讀音群組。相同群組只保留一個代表 target；不同讀音、不同詞義／詞組或不同 TTS 條件不得合併。
5. 用最少的完整代表句覆蓋全部讀音群組；同一句可承載多個群組並在 `targets[]` 分列。同一代表句內同群組重複出現時只留一個 target，不要求重複實聽。
6. 核對 `displayText`、`ttsInput`、正確讀音、臺灣注音、熟悉同音字、讀音群組用法、`cueMode`、revision 與 fingerprints。
7. 將減量後的代表句一次批次加入 `src/data/guwenPronunciationAudit.ts` 正式 catalog。網頁的臨時多行貼入欄只供本機試聽，不算正式上傳。
8. 執行規定的測試、build、commit、push 與 GitHub Pages 部署。
9. 開啟正式實聽台，確認：
   - 中央資料庫已連線。
   - 正式網站版本包含本次 commit。
   - 該篇全部正式候選都已出現。
   - 同句多目標各有獨立「念對／念錯」控制，且全部選完才回傳。
10. 正式 catalog 移除的舊項目若從未有實聽結果，可從中央 catalog 與本機顯示清單退役；已有任何結果者或使用者臨時自訂句必須保留，不得刪除。
11. 不得播放後自行判定，也不得替使用者按「念對／念錯」。

### 3.3 第一階段交接格式

完成後向使用者回報：

- 篇次、`lessonId`、篇名與教材主檔。
- 正式實聽台網址。
- 白名單候選語音單元數、減量後代表句數。
- 不重複讀音群組／代表 target 總數。
- 逐語音單元的完整句子與 targets 清單。
- 中央 catalog／連線狀態。
- 部署 commit SHA 與部署結果。
- 是否有任何尚未能正式建檔的文字。

然後停止。清楚告訴使用者：

1. 請在孩子實際使用的裝置逐句播放。
2. 每個代表 target 都要選「念對」或「念錯」；相同讀音群組不需重複聽。
3. 每次要看到「已回傳中央資料庫」及回傳編號。
4. 全部完成後，只要回到這個對話輸入第二階段短指令。

Agent、自動測試、瀏覽器腳本和 TTS 輸入文字都不能聽見實際發音，不得產生假的聲音判定。

## 四、使用者實聽

使用者在正式實聽台：

1. 確認篇章與實際 `zh-TW` voice。
2. 逐句按「播放」並親耳聽完完整句子。
3. 對句中的每個多音字 target 分別選「念對」或「念錯」。
4. 同句多目標全部選完後，確認中央回傳成功與回傳編號。
5. 若回傳失敗，使用「重新同步全部結果」補送；只留在 localStorage 不算完成。
6. 實聽台必須提供「全部／還沒聽過／已經聽過」清單篩選；「已經聽過」包含念對與念錯，「還沒聽過」只包含仍為 pending 的句項。
7. 「依序播放目前清單」只播放當前篩選結果。使用者選「還沒聽過」後，不得再次混入已完成句項。
8. 第二階段依真人結果新增的「讀音提示」本身不遞迴建立新的多音字 audit target；否則會形成無限送測循環。提示 TTS 仍須依顯示／朗讀分離規格做自動化驗證。

這是唯一不能由 agent 代勞的階段。

## 五、第二階段：讀回結果並套用提示

收到第二階段觸發句後，agent 必須：

1. 重新同步固定分支並重讀該篇主檔、共用狀態及本 Runbook。
2. 執行：

   ```bash
   npm run tts:audit:pull -- --lesson <lessonId>
   ```

3. 確認中央輸出的篇次、篇名、`lessonId` 與本篇完全相符。
4. 逐 target 核對 `displayText`、`ttsInput`、target 位置、revision、fingerprints 與適用 voice 環境。
5. 只使用目前有效的代表 target-level 結果：
   - `念對`：該讀音群組的所有白名單朗讀位置都不顯示讀音提示；若已有專為它加入的提示則刪除。
   - `念錯`：該讀音群組所有白名單朗讀位置都保留或新增規定格式的正確讀音提示。
   - `待實聽`、`待複驗`、stale、missing、回傳失敗：不得自行決定；列出未完成項目並停止該 target 的教材結論。
   - 若實作文字、詞義／詞組、`ttsSafe()` 或 voice 條件與原群組不同，不能套用代表結果，必須拆組重測。
6. 同一句若一字念對、一字念錯，只移除念對字的提示，保留念錯字的提示。
   - 若使用者在備註中明確指出「按錯 target，真正錯的是同句另一字」，第二階段必須保留中央原始紀錄並另做字級歸因校正。
   - 備註或使用者後續說明已清楚指明哪個字念對、哪個字念錯時，以這個真人字級判定為正式結論；不得把誤按 target 當成真正念錯。
   - 若說明仍有歧義，或真正念錯的字尚未建立可對應讀音群組，該 target 保持未完成並請使用者補聽，不得由 agent 猜測。
7. 不得改寫古文原句、真實古文線索或正式句子來規避發音；可見注音與提示 TTS 文字必須依既有規格分開。
8. 同步教材主檔、App 正式資料、正式 catalog／實聽表、`GUWEN-PROJECT-STATUS.md` 與 SOP 指定的其他來源。
9. 執行完整驗證、commit、push、部署及正式站驗收。

### 第二階段完成回報

必須回報：

- 測試語音單元數與 target occurrence 數。
- 哪些 target 有效念對，因此不加註或已移除提示。
- 哪些 target 有效念錯，因此保留或新增哪一行提示。
- 哪些 target 仍待實聽、待複驗、stale、missing 或回傳失敗。
- 修改檔案、測試結果、部署狀態與最終 commit SHA。

只要還有未完成 target，就不得宣稱全篇多音字處理完成。

## 六、文字改動後的重測規則

第一階段部署後，只要 `displayText`、`ttsInput`、target 位置、指定讀音、revision、`ttsSafe()` 行為或適用 voice 條件改變：

1. 舊結果必須失效為待複驗。
2. 重新執行第一階段的候選更新、部署與使用者實聽。
3. 不得把另一句、另一位置或另一篇的同字結果當成永久免測依據。

## 七、百篇文章的日常用法

每篇對話只需要這兩次互動：

```text
文章完成
→「第 N 篇已經定稿。請執行正式多音字實聽流程第一階段。」
→ Agent 建正式批次、部署並停下
→ 使用者在實聽台完成真人判定
→「第 N 篇已經全部實聽完成。請讀回中央結果並套用讀音加註。」
→ Agent 讀回、增刪提示、驗證並部署
```

第二階段最好回到原對話執行；若原對話無法使用，任何讀到本 repository 最新狀態的新 agent 也能以相同 `lessonId` 從中央資料庫接手，不需要使用者重新口述每個結果。
