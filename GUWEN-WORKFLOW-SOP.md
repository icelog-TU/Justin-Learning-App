# 《古文破譯家》多對話協作與版本管理 SOP

> 文件用途：規定每一篇新對話如何開始、逐批審稿如何進行、哪些檔案要更新，以及如何避免不同對話各自產生不同版本。  
> 適用範圍：教材選篇、內容編寫、成人審稿、密碼鑰匙管理、App 實作交接。  
> 本文件是固定流程；即時進度另見 `GUWEN-PROJECT-STATUS.md`。  
> 最近更新：2026-07-26

---

## 一、基本架構：一個專案，多個短對話

1. 整個《古文破譯家》只使用一個 ChatGPT 專案。
2. 原則上一個對話處理一篇古文；內容較短時可處理兩篇。
3. 對話變長、開始讀取緩慢或訊息不易送出時，不在原對話勉強延續，改開同專案內的新對話。
4. 新對話不靠重讀所有舊對話接手，而是讀取專案共同指示與下列共用檔案。

---

## 二、四種「唯一真實來源」

| 要確認的事情 | 唯一真實來源 |
|---|---|
| 教材長期不變的設計原則 | `design-standard.md` 與專案共同指示 |
| 整個專案目前做到哪裡 | `GUWEN-PROJECT-STATUS.md` |
| 某篇教材目前實際寫了什麼 | 該篇唯一的教材主檔 |
| 哪些鑰匙已正式取得、提醒過幾次 | `guwen-decoder-learned-keys.md` |
| 哪些完整語音單元待測、念對或念錯 | `src/data/guwenPronunciationAudit.ts`＋Firestore 中央實測庫 |

補充：

- `GUWEN-NEW-CHAT-HANDOFF.md` 是歷史接手說明，可供理解舊專案背景；建立本 SOP 與總進度表後，不再負責承載每日變動的最新進度。
- 日期 ZIP 是階段性備份快照，不是日常工作的來源。
- `app-implementation-contract.md` 只在核准內容準備交付程式實作時使用。
- 上表所稱的教材主檔、總進度表、鑰匙庫與共用規範，日常工作一律以 GitHub 指定 repository／工作分支上的現行版本為準；聊天附件、下載檔、ZIP 與本機暫存副本不得取代 GitHub 正式檔。
- 若共用文件互相矛盾：古文原文與題目內容以當篇教材主檔為準；核准狀態以教材主檔中的明確標記及使用者在對話中的明確核准為準，不得自行推定。

### GitHub SOP 同步頻率

1. GitHub 指定 repository／工作分支上的 `GUWEN-WORKFLOW-SOP.md` 是本專案 SOP 的唯一最新版（Single Source of Truth）。
2. 每次開始新的古文篇章、新的接手討論串，或對中斷已久的工作重新接手時，必須先從 GitHub 讀取最新版 SOP，再開始閱讀進度表、鑰匙庫與教材主檔。
3. 同一討論串完成這次同步後，後續回合沿用本次已讀取的 SOP，不需要每一則訊息都重新連線讀取 GitHub。
4. 只有在下列情況才需要重新同步 SOP：
   - 使用者明確表示 SOP 已更新；
   - 使用者要求「重新同步 GitHub」；
   - 其他討論串或程式代理剛提交了 SOP 修改；
   - 現行工作內容與已讀 SOP 出現矛盾或版本疑慮。
5. 多個討論串可以同時讀取同一份 SOP；純讀取不會鎖住檔案，也不會互相衝突。若要修改同一份 SOP，則必須依序進行，且每次寫入前重新讀取遠端最新版與目前 SHA，避免覆蓋其他討論串的新提交。
6. 若 GitHub Connector 暫時無法使用，不得宣稱已完成最新同步。應明確告知目前只能使用最後一次成功讀取的版本；涉及 SOP、核准狀態或共用檔案的重要變更，待連線恢復並重新同步後再提交。

---

## 三、每篇新對話開始前必讀

新對話開始時，先從 GitHub 指定 repository／工作分支讀取最新檔案，不沿用聊天附件或先前下載副本。依序閱讀：

1. 專案共同指示。
2. `GUWEN-WORKFLOW-SOP.md`。
3. `GUWEN-PROJECT-STATUS.md`。
4. `design-standard.md`。
5. `guwen-decoder-learned-keys.md`。
6. 只要本輪會新增或修改任何孩子端播放文字、TTS、讀音提示或多音字資料，完整閱讀 `GUWEN-TTS-CENTRAL-DATABASE-HANDOFF.md`。
7. `src/data/guwenPronunciationAudit.ts`，並執行 `npm run tts:audit:pull` 取得中央實測結果。
8. 本次要處理的教材主檔。
9. 需要核對固定範例或格式時，才回查前篇主檔。
10. 準備交付 App 實作時，再讀 `app-implementation-contract.md`。

讀完後先向使用者確認，不立刻大量續寫：

- 本次處理的篇名與主檔名稱；
- 目前已核准到哪裡；
- 哪些內容仍是待審草稿；
- 本輪只處理哪一小段；
- 本篇可能調用哪些舊鑰匙。

### 新對話固定開場 Prompt

> 這個對話處理第○篇《○○○》。  
> 請先閱讀專案共同指示、`GUWEN-WORKFLOW-SOP.md`、`GUWEN-PROJECT-STATUS.md`、`design-standard.md`、`guwen-decoder-learned-keys.md`，以及本篇教材主檔。  
> 如果會新增或修改任何孩子端播放文字、TTS、讀音提示或多音字資料，也必須先完整閱讀 `GUWEN-TTS-CENTRAL-DATABASE-HANDOFF.md`，並執行 `npm run tts:audit:pull`。
> 先告訴我目前已核准到哪裡、哪些內容仍待審、本輪準備處理哪一小段，以及會遇到哪些舊鑰匙；不要立刻大量續寫。

---

## 四、每一批教材的工作流程

### A. 開始本批

1. 成人逐題編寫或審稿時，每一題都必須在該題最上方獨立重貼本篇採用的古文全文，不得改字；不得只在整批第一題重貼一次。
2. 同一批連續呈現兩題以上時，第二題及後續每一題仍須再次重貼全文，讓成人編輯者不必回捲或憑記憶核對。
3. 寫入教材主檔供 GitHub 逐題審稿時，每個正式題號錨點之後、題目內容之前，也要放置「本篇完整原文（成人審稿用）」；這段是成人定位資訊，不自動成為孩子的 App 題目畫面。
4. 重貼全文後，標示本題正在處理的目標句或片語。
5. 說明本題預計新增或修改的推理任務。
6. 若要修改已核准內容，先提出「原版／建議版／修改理由／學習影響」，等待使用者同意。

### B. 撰寫本批

1. 依設計規範維持「古文破譯家」完整角色任務。
2. AI 交付古文線索，孩子比較證據並提出假說。
3. 每題送審前檢查角色高低：AI 必須先交代自己哪裡拿不準，再拜託古文破譯家幫忙判斷、追蹤或重建；不得以老師發布「比較任務」的口吻命令孩子作答，也不得只機械補上一個「請」字。
4. 孩子端任務標題優先使用「麻煩古文破譯家幫忙」或符合情境的求助語；選項只作為專家提交解法的介面。
5. 取得理解後才稱為「密碼鑰匙」。
6. 多個鑰匙組合成原句畫面時，另設具有連續題號的正式互動題。
7. 孩子端兩條證據一律只標示為「線索一」「線索二」；不得顯示或朗讀「真實古文」「混種古文」「AI 仿古」「線索類型與出處」等成人分類，App 引導語也不交代尋找或改寫線索的製作過程。
8. 每條線索的真實原句、作者／篇名、類型及教材改寫範圍，集中記在題目後方的「成人編輯備註（不進入孩子端、不朗讀）」。
9. 新寫內容一律標示為「草稿／待審」，不得自行標成「核准」。

#### 作答前提示必須保留開放思考

1. 引導語的功能是交代故事位置、指出待破解字詞並邀請孩子觀察證據，不是預先列出答案範圍。
2. 在孩子比較古文線索以前，不得把可能答案寫成二選一或多選一的口頭提示，例如「是往前還是回去」、「是開始、進行還是結束」。
3. 不得先替目標字詞分類，例如直接說它是「時間詞」、「方向動作」、「人物稱呼」或「因果關係」。這類分類本身可能已經排除其他假說。
4. 引導語、線索白話、圖片說明、語音與推理提問都要接受同一項檢查：若孩子還沒比較線索，就能只靠提示排除大部分可能，便屬於過度提示。
5. 優先使用中性的觀察任務，例如：
   - `看看兩條線索中，人物的位置前後發生了什麼變化。`
   - `比較「罷」前後的畫面，再提出你的假說。`
   - `看看兩條線索中的「及」怎麼把前後的事情接起來。`
6. App 仍可在證據呈現後提供三個假說選項，讓孩子提交判斷；但選項只是提交介面，不得反過來主導前面的思考。孩子應先有機會從線索形成較開放的理解，再比較哪個假說最受證據支持。
7. 第一次答錯後可以指出需要重新觀察的具體證據，但仍不得直接改寫正解。

#### 選擇題正解位置必須打亂

1. 每寫完一題，先列出本篇截至目前的正解位置序列，例如 `1、2、2、2、1、3`；不得只檢查當題。
2. 不得把正解慣性放在第一項，也不得長段固定在第二項或第三項。
3. 不得形成全奇數、全偶數、固定輪替、連續遞增或其他容易猜出的規律。
4. 調整位置後，同步更新正解編號、答錯提示、逐項詳解、資料欄位、測試與解鎖條件。
5. 成人主檔的每一題都要留下「選項位置檢查」；每批送審前再做一次全篇分布稽核。
6. 多選題的正確項與不應勾選的項目要交錯排列，不得集中。

7. 每篇收尾的全文證據檢查只能有兩類：「文章有明確寫出來」與「文章沒有明確寫出來」。合理推論也歸入後者，不另設第三類。孩子只勾選文章有明確寫出的敘述；詳解可在作答後說明哪些未勾內容是合理推論，但不得把這項差異變成孩子端的分類負擔。

### C. 使用者審稿

使用者可能作出三種決定：

- **核准**：可以寫入主檔並標示已核准。
- **修改後核准**：依指定修改完成後，才標示已核准。
- **待審／退回**：保留草稿狀態，不得計入已完成範圍。

沒有得到明確核准，不可因為內容已寫完、已討論過或使用者未反對，就自行視為定稿。

### D. 本批結束

依實際情況更新：

1. 當篇教材主檔：寫入最新內容與段落狀態。
2. `GUWEN-PROJECT-STATUS.md`：更新目前進度、核准邊界、下一步與日期。
3. `guwen-decoder-learned-keys.md`：只在內容已核准且鑰匙正式取得後更新；待審草稿不得先登記。
4. 若教材已全文核准並準備實作，再更新 App 實作／驗收狀態。

每次回覆使用者時，應清楚區分：

- 這次新增了什麼；
- 哪些已核准；
- 哪些仍待審；
- 下一步是什麼。

---

## 五、總進度表如何使用

`GUWEN-PROJECT-STATUS.md` 全專案只有一份，不為每個對話另建副本。

### 每個對話都要做的事

- 開始工作前讀取同一份總進度表。
- 有實質進度變化時，更新同一份總進度表。
- 若只是在討論想法、沒有改變教材或核准狀態，不必更新。

### 需要更新的時點

- 新篇主檔建立；
- 新增一批待審草稿；
- 使用者核准或退回一批內容；
- 某篇全文核准；
- 鑰匙庫完成同步；
- 教材交付 App 實作；
- App 實作或驗收完成；
- 下一步或工作中的篇章改變。

### 進度表不保存什麼

- 不貼完整題目；
- 不保存長篇討論理由；
- 不取代教材主檔；
- 不把「已寫完」誤寫成「已核准」；
- 不記錄尚未發生的預計進度為完成。

---

## 六、檔名與版本管理

### 1. 同一篇只保留一個固定主檔名

格式：

`NN-guwen-拼音或識別名-decoder-content.md`

例如：

`08-guwen-changganrucheng-decoder-content.md`

禁止建立：

- `第八篇新版.md`
- `第八篇最新版2.md`
- `第八篇最終版.md`
- `第八篇真的最終版.md`

### 2. 修改既有檔案，不另開平行版本

- 內容更新時，替換同名主檔。
- 專案來源區若已存在舊檔，先確認並移除舊檔，再加入更新後的同名檔；不要選擇「仍然上傳」而留下兩個同名來源。
- 對話中的附件或下載檔不會自動等於專案來源已更新；完成一批工作後，要確認專案「來源」區看到的是最新版本。

### 3. 用檔案內狀態標記版本，不用改檔名

主檔開頭至少標示：

- 文件狀態；
- 已核准範圍；
- 待審範圍；
- 最近更新日期；
- 下一批範圍。

題目標題使用：

- `（已核准）`
- `（修正版待審）`
- `（草稿待審）`

### 4. 每篇教材主檔必須有檔案內快速目錄

每一篇教材主檔都必須使用 Markdown 檔案內超連結，方便在 GitHub 或其他支援錨點的 Markdown 預覽器中快速審稿。ChatGPT Android App 目前可顯示 Markdown 連結，但不能可靠執行檔案內錨點跳轉，因此不得把 ChatGPT 檔案預覽當作本專案的正式跳轉審稿環境。

#### 必備結構

1. 檔案最上方設定固定首頁錨點：`<a id="top"></a>`。
2. 文件狀態之後設定固定題目目錄錨點：`<a id="question-index"></a>`。
3. 最上方建立「標題快速目錄」，至少包含：
   - 文章基本資料；
   - 選文理由；
   - 舊鑰匙盤點；
   - 新密碼候選；
   - 題目快速目錄；
   - 白話驗證卷軸；
   - 完成鼓勵與徽章；
   - 成人編輯備註；
   - 審稿狀態。
4. 題目快速目錄必須逐題列出所有正式題號，不得只列已核准題目。
5. 每一題使用固定、簡短、不因標題文字修改而改變的英文數字錨點：
   - 第一題：`<a id="q01"></a>`
   - 第二題：`<a id="q02"></a>`
   - 依此類推。
6. 每一題內容結束處都要加入：`[↑ 返回題目快速目錄](#question-index)`。
7. 章節也應使用明確的英文錨點，不依賴預覽器自動產生的中文標題網址，以避免中文、標點、重複標題或平台規則不同造成連結失效。
8. 新增、刪除、拆分或重編題號時，必須同步更新：
   - 最上方題目快速目錄；
   - 題目前的錨點；
   - 每題返回目錄的連結。
9. 檔案完成修改後，至少檢查一次：
   - 目錄中的每一題都有對應錨點；
   - 每個題目錨點只出現一次；
   - 每一題結尾都有返回目錄連結；
   - 目錄沒有連到不存在或已刪除的題號。

#### 標準範例

```md
<a id="top"></a>

# 古文破譯家｜《篇名》教材主檔

<a id="question-index"></a>

## 題目快速目錄

[第一題](#q01)｜[第二題](#q02)｜[第三題](#q03)

---

<a id="q01"></a>

# 第一題（草稿待審）｜破解「○」

題目內容……

[↑ 返回題目快速目錄](#question-index)
```

### 5. 日期 ZIP 只作備份

在下列里程碑建立完整 ZIP：

- 一篇全文核准；
- 鑰匙庫大量更新；
- 準備交付程式實作；
- 專案規範重大改版。

命名格式：

`guwen-project-backup-YYYY-MM-DD.zip`

ZIP 建立後不繼續在壓縮檔內直接工作。

---

## 七、GitHub 日常審稿與提交規則

### 1. GitHub 是正式工作位置

1. 《古文破譯家》的教材主檔、SOP、總進度表與鑰匙庫，都以指定 GitHub repository 的現行工作分支為正式版本。
2. 每一篇教材從建立主檔開始，就直接放在 GitHub；不得等全文完成後才上傳。
3. 使用者以 GitHub 網頁直接閱讀與審稿，並使用檔案內快速目錄跳轉。
4. ChatGPT 對話中的附件、sandbox 連結、手機下載檔與臨時工作副本只可作暫存或備份，不得被稱為正式主檔。

### 2. 每輪開始前先讀取遠端最新版

1. 修改任何檔案前，先從 GitHub 重新讀取目標工作分支上的現行檔案。
2. 不得直接拿舊附件覆蓋 GitHub；若本地副本與遠端不同，先比較差異。
3. 同一檔案可能由其他對話或程式代理更新時，提交前必須再次核對遠端版本，避免覆蓋別人的新修改。
4. 若 repository、分支或檔案路徑尚未確認，先做只讀查詢；不得自行猜測後寫入錯誤位置。

### 3. 每次改寫後直接更新同一份 GitHub 主檔

1. 使用者核准、要求修改，或本輪產生其他實質進度後，直接更新 GitHub 上同一個固定檔名，不要求使用者下載再手動上傳。
2. 一輪若同時影響教材主檔、總進度表、鑰匙庫或 SOP，應在同一輪同步更新所有受影響檔案。
3. 每次更新必須建立清楚的 Git commit；commit 訊息應簡短說明篇名與本輪內容。
4. 不得用改檔名的方式區分版本，不得建立「最新版」「修正版2」或帶日期的平行正式檔。
5. 若 GitHub 工具或寫入權限當下不可用，應明確告知使用者尚未提交；不得只保存本地副本後宣稱 GitHub 已更新。

### 4. 提交後必須遠端回讀驗證

提交完成後，重新從 GitHub 讀取正式檔案並確認：

- 修改內容確實出現在正確 repository、工作分支與固定檔名；
- 已核准、待審與下一步的狀態沒有誤改；
- 標題快速目錄、逐題錨點與每題返回連結完整；
- 沒有誤建第二份平行主檔；
- 相關共用檔案已按本輪實際變化同步。

不得只因寫入工具回報成功，就省略遠端回讀。

### 5. 每輪交付必須提供 GitHub 直達連結

1. 完成提交與回讀後，回覆使用者可直接打開的 GitHub Markdown 網頁連結。
2. 連結必須指向本輪實際更新的 repository、工作分支與正式檔案，不得只給 sandbox 下載連結。
3. 若本輪更新多份檔案，至少提供教材主檔連結；SOP、進度表或鑰匙庫有實質變更時，也應一併提供對應連結。
4. GitHub 連結是日常審稿入口；使用者不必重複下載檔案，也不必手動重新上傳。
5. 手機審稿時應以瀏覽器開啟 GitHub 網頁；檔案內錨點跳轉以 GitHub 的實際支援為準。


### 6. 對話回覆採「重點＋GitHub 連結」，不重貼全文

1. GitHub 主檔已可直接開啟時，ChatGPT 對話只回報本輪核准狀態、關鍵修改、待決問題與直達錨點；不再把整題或整篇教材全文貼回對話。
2. 每一題完整古文原文、題目內容、來源核對與語音稽核仍必須完整保留在 GitHub 主檔；「不在對話重貼」不等於刪除主檔中的成人審稿脈絡。
3. 本規則優先於先前「每批在對話完整重貼原文」的呈現習慣。只有使用者明確要求、GitHub 無法存取，或需要比較兩個短版本時，才在對話貼出必要片段。
4. 回覆至少提供教材主檔的本題錨點；若 SOP、設計規範、進度表或鑰匙庫有修改，也一併提供直達連結。

---

## 八、避免不同對話互相覆蓋

1. 同一時間原則上只讓一個對話修改同一篇教材主檔。
2. 不同對話可同時討論不同篇，但都要先讀同一份總進度表與鑰匙庫。
3. 若 A 對話剛更新共用檔案，B 對話繼續工作前要重新讀取最新檔案。
4. 不從聊天記憶猜測目前核准狀態；看主檔與總進度表。
5. 發現主檔、進度表、鑰匙庫內容不一致時，先停止續寫，列出衝突請使用者確認。
6. 不可默默合併兩個互相衝突的版本。

---

## 九、舊鑰匙庫更新規則

### 新版從第一篇重寫時的計次隔離

1. 若教材宣布從第一篇開始重新編寫或重新核准，必須建立一條新的有效學習時間線。
2. 舊版教材、後續篇章與歷史提醒次數只可作成人比對資料，不得倒灌成新版第一篇孩子已取得的鑰匙。
3. 新版第 N 篇只能調用新版第 1 至 N−1 篇已重新核准的鑰匙；同篇稍早題目取得的鑰匙，則必須在取得題核准後才可於後題調用。
4. 待審草稿、舊版核准內容與尚未按新版篇序重審的後續篇章，都不構成有效舊鑰匙。
5. 發現新舊時間線混用時，立即停止續寫，將誤用題改回首次破解或提醒題，並同步修正主檔、進度表與鑰匙庫基準。

1. 編寫新篇前先查鑰匙庫。
2. 同字同用法：直接調用，依提醒次數決定是否給短提示。
3. 同字不同用法：另立新鑰匙，不可混在原紀錄中。
4. 待審題目不得先寫入正式鑰匙庫。
5. 使用者核准相關題目後，才登記新鑰匙與實際發生的舊鑰匙提醒。
6. 同一把鑰匙在同一後續篇章最多計一次提醒。
7. 第五次後標示熟練，下一篇起不再強制提醒。

---

## 九之一、逐題掃描＋全篇批次多音字查核流程

百篇教材的對話觸發、兩階段停點、固定回報格式與接手方式統一見 `GUWEN-POLYPHONIC-AUDIT-RUNBOOK.md`。使用者說「第 N 篇已經定稿」並要求正式實聽時執行第一階段；使用者說「第 N 篇已經全部實聽完成」並要求讀回時執行第二階段。意思相同的自然語句也算觸發，不得要求使用者重新貼長版指示。本節以下內容仍是實作時必須遵守的正式規則。

1. 每一道題編寫或修改完成後、送交成人編輯者審核以前，先掃描本題全部孩子端可播放文字，避免編寫途中遺漏。
2. 當一整篇古文的任務開場、原文播放、App 引導語、古文線索、破解白話、任務／題目、選項、答對回饋與答錯提示全部寫完後，必須再從頭做一次全篇最終掃描與減量。這是交付前的獨立關卡，不能用前面的逐題掃描取代。
3. 最終掃描只限上述確實會朗讀的白名單，並抓出其中全部多音字，不得只挑 agent 猜測「可能念錯」或「比較難」的字。詳解、成人編輯資料、出處、學習目標、證據邊界、故事畫面、排序結果、白話卷軸、完成／徽章訊息及其他只顯示不播放文字一律排除。
4. 開始本批前先執行 `npm run tts:audit:pull`，讀取中央多音字實測庫。不得只根據聊天記憶、本機勾選或同字在另一句的結果判斷。
5. Agent 先合併完全相同的句子，再按「字形＋指定讀音＋會影響讀音的詞義／詞組用法＋相同 `ttsSafe()`／voice 條件」分組，以最少代表句覆蓋全部群組後批次加入 `src/data/guwenPronunciationAudit.ts`。同群組只聽一次；不同讀音、用法或 TTS 條件不得合併。同一代表句可在 `targets[]` 承載多個群組。`cueMode` 仍須區分已知用法與尚待破解目標。
6. 提交、部署並至少開啟一次正式 `/#/tts-audit`，讓這批 catalog 出現在網頁與中央資料庫。只把多行貼進網頁會形成本機「待分類」句，不能算正式批次上傳或正式候選。
7. 使用者在孩子實際使用的裝置逐句播放完整整句；只有使用者能依耳朵替該句的每個多音字位置分別點「念對／念錯」。Agent、文字模型、型別測試與瀏覽器自動化都聽不到實際發音，不得替使用者作出聲音判定。
8. 同一句有多個多音字時，一句只播放一次，但每個目標分開判定；全部目標選完後，網頁才把含有逐目標 `targetResults[]` 的結果回傳 Firestore。畫面必須顯示「已回傳中央資料庫」及回傳編號；失敗時只算本機待重送，不算完成。
9. 某一代表目標的有效結果為「念對」時，套用到同一已核准讀音群組的所有白名單朗讀位置；不得影響同句其他群組。
10. 某一代表目標的有效結果為「念錯」時，同一已核准讀音群組的每個白名單朗讀位置都必須就近加入提示；不同詞義／詞組、TTS 輸入或 voice 條件必須拆組重測。待實聽、回傳失敗或待複驗者維持候選狀態，不得假稱念對或念錯。
11. 非待破解目標的提示固定為：`度，當作「測量」時，念作墮（ㄉㄨㄛˋ）。`；若多音字或所在詞語正是待破解目標，作答前固定為：`度，這裡念作墮（ㄉㄨㄛˋ）。`，不得以字義洩題。沒有孩子熟悉的同音漢字時，可用熟悉詞語，例如：`看，當作「看到」時，念作「看見」的看（ㄎㄢˋ）。`
12. 每個需要顯示的提示另存 TTS 朗讀文字：畫面保留括號注音；朗讀文字移除括號及其中注音，但必須念出其餘國字提示，不得跳過整段。
13. 現代中文可以自然避開時可先提出改寫；本篇古文原文與真實古文引文不得為了讀音而改字。
14. 修改完整語音單元、`ttsSafe()` 輸入、目標字位置、指定讀音或 voice 驗收條件後，舊實測不得直接沿用；標成待複驗並重新送測。
15. 使用者實聽完成後，該篇古文對話執行 `npm run tts:audit:pull -- --lesson <lessonId>`；也可用篇次或完整篇名取代 lessonId。讀回輸出會逐目標列出「不加註／必須加註」與建議提示文字，再由 agent 同步中央 Firestore 結果、正式候選檔、教材主檔實聽表與孩子端提示。四者不一致時停止交付。
16. App 實作後仍須逐句實聽並成功回傳。只有掃描表、只有 catalog、只有本機勾選、或只有 TTS 程式替換測試，都不算完成。
17. 中央歷史可供未來統計哪些字、句型或 voice 經常念對，但目前不得把跨句統計自動變成永久免測。只有目前完整整句、TTS 輸入、目標位置、revision 與適用語音環境都有有效結果時才可沿用；未來若要建立免測政策，必須另行實作並由使用者核准。

### 中央資料庫的固定讀寫責任

1. 實聽台的中央資料與孩子的 `AppData`、獎勵及學習同步碼完全分離。
2. 網頁負責寫入與讀回；`localStorage` 只作離線備份和待重送佇列。
3. 編輯代理負責在每次編題前執行 `npm run tts:audit:pull`，再依結果更新正式候選檔和教材主檔。
4. 每篇至少保存：篇次、篇名、題號、語音單元 ID、完整文字、目標字與出現次序、正確注音、同音提示、句義、裝置／聲音、實聽日期、結果與回傳編號。
5. 同一篇的新結果累積在該篇中央紀錄中；不得另建名稱相近的第二套多音字清單。

### 多音字 TTS 實聽台的固定入口與用法

1. 正式入口固定為 <https://icelog-tu.github.io/Justin-Learning-App/#/tts-audit>；它不在孩子主選單內，編輯者直接開啟網址。
2. 新增或修改的語音單元必須先進入 `src/data/guwenPronunciationAudit.ts` 正式候選檔；網頁臨時貼入的「待分類」句只留在本機，不能當成中央正式資料。
3. 在孩子實際使用的手機、平板或電腦上開啟實聽台，先確認畫面顯示中央資料庫已連線與目前實際選擇的 `zh-TW` 聲音。
4. 逐句按「播放」，實際聽完後才按「念對」或「念錯」；不得由 agent 猜測或代替真人實聽。
5. 每次送出都必須看到「已回傳中央資料庫」與回傳編號。若顯示失敗，只代表本機暫存，必須重送成功才算完成。
6. 實聽完成後由 agent 再執行 `npm run tts:audit:pull`，確認每個目標的「不加註／必須加註」、建議提示文字、待複驗、中央缺少與孤兒紀錄；只有與目前完整文字、TTS 輸入、目標位置及 revision 指紋完全相符，且全部目標都有判定的結果才能形成教材結論。
7. 完整工程說明、資料 schema、故障排除與驗收清單以 `GUWEN-TTS-CENTRAL-DATABASE-HANDOFF.md` 為唯一詳細交接文件；不得另建重複規格。
8. 對話操作與兩階段交接以 `GUWEN-POLYPHONIC-AUDIT-RUNBOOK.md` 為唯一簡式入口；Runbook 不取代本 SOP 或中央資料庫技術交接。

---

## 十、從教材轉交 App 實作

只有當相關教材內容已核准，才可進入實作。

交付前確認：

1. 主檔中核准範圍清楚。
2. 古文原文與真實古文線索逐字固定。
3. 全篇孩子端文字已完成多音字掃描。
4. 鑰匙庫與提醒次數已同步。
5. 白話驗證、證據檢查、徽章收集流程完整。
6. 實作代理已閱讀 `app-implementation-contract.md`。

程式實作不得為配合資料結構而擅自刪改、合併或重排已核准教材。

---

## 十一、目前建議的專案來源清單

### 固定共用文件

- `GUWEN-WORKFLOW-SOP.md`
- `GUWEN-POLYPHONIC-AUDIT-RUNBOOK.md`
- `GUWEN-PROJECT-STATUS.md`
- `design-standard.md`
- `guwen-decoder-learned-keys.md`
- `src/data/guwenPronunciationAudit.ts`
- `app-implementation-contract.md`
- `GUWEN-NEW-CHAT-HANDOFF.md`（歷史接手參考）

### 教材主檔

- 每篇各一份固定檔名的 Markdown 主檔。

### 備份

- 日期 ZIP 可保留一份最新里程碑備份；不必把每次小修改都另存一個 ZIP。

---

## 十二、每輪結束檢查表

- [ ] 本篇古文原文沒有被修改。
- [ ] 新增或修改的每一題已在送審前完成孩子端多音字掃描；每個實際位置、注音、同音提示與 TTS 驗收項目均已登記。
- [ ] 非目標多音字與待破解目標已分別採用指定的兩種提示格式。
- [ ] 每個讀音提示已另存「移除括號注音、只念國字」的 TTS 朗讀文字，並列入主檔修改索引。
- [ ] 本篇正解位置序列已記錄並檢查，沒有固定位置或可猜規律；調序後的正解、提示與詳解已同步。
- [ ] 成人逐題審稿時，每一題最上方都已獨立重貼完整本篇古文；同批後續題目沒有省略。
- [ ] 新增內容的「草稿／待審／已核准」標記正確。
- [ ] 未經使用者核准的內容沒有被視為定稿。
- [ ] 作答前提示沒有預列答案範圍、替目標分類或把開放推理縮成二選一。
- [ ] 孩子端只顯示「線索一／線索二」，來源類型、真實原句與改寫範圍只保留在不朗讀的成人編輯備註。
- [ ] 修改前已從 GitHub 讀取目標工作分支的遠端最新版。
- [ ] 當篇主檔已直接更新並提交到 GitHub 的固定正式檔名。
- [ ] 有實質進度變化時，總進度表已更新。
- [ ] 只有正式核准的鑰匙才寫入鑰匙庫。
- [ ] 每篇主檔的標題快速目錄、逐題錨點與返回目錄連結完整且互相對應。
- [ ] GitHub 上沒有同名或「最新版2」之類的平行正式檔案。
- [ ] 提交後已從 GitHub 遠端回讀，確認內容、狀態與導航連結。
- [ ] 已提供本輪正式 Markdown 檔的 GitHub 直達連結。
- [ ] 對話回覆只保留重點與 GitHub 直達連結，未在使用者未要求時重貼整題或整篇全文。
- [ ] 已向使用者說明本輪結果與下一步。
- [ ] 實聽台可分開顯示「還沒聽過」與「已經聽過」，依序播放不會混入篩選外句項。
- [ ] 若真人備註指出誤按 target，已同時保存中央原始紀錄與正式字級校正，沒有替其實念對的字加註。
