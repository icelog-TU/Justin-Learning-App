# 《古文破譯家》工作流程

> 用途：決定每種任務要讀什麼、以哪個檔案為準、何時更新與何時重新讀取。
> 固定分支：`claude/chinese-learning-app-justin-yjcfam`

## 1. 權威順序

發生衝突時依序採用：

1. 使用者目前任務中的明確指示。
2. 遠端固定分支最新版。
3. 當篇被 `GUWEN-PROJECT-STATUS.md` 標為 `active` 的唯一教材主檔及其中的明確核准紀錄。
4. 本工作流程與任務對應 skill。
5. 現行程式碼、型別與測試；App 實作時用來決定技術做法，不得覆蓋已核准教材文字。
6. 格式、鑰匙庫及按需 reference。
7. 封存教材、歷史 handoff、舊 catalog、stage 報告與 Git 歷史。

聊天附件、舊 ZIP、其他本機副本與對話記憶不得覆蓋遠端 active 主檔。

## 2. 先選任務路徑

### A. 教材設計、撰寫或審稿

固定讀：

1. 本檔。
2. `.claude/skills/design-guwen-decoding/SKILL.md`。
3. 當篇 active 教材主檔。

按需讀：

- `GUWEN-PROJECT-STATUS.md`：只讀當篇一列及本輪下一步。
- `guwen-decoder-learned-keys.md`：按目標字詞、篇名或鑰匙搜尋，不全文讀。
- `design-standard.md`：新篇、全新題目、教學方法改動或全文審查時讀。
- `GUWEN-MARKDOWN-FORMAT.md`：新建主檔、改標題／層級／題型結構或格式檢查失敗時讀。
- `canonical-example.md`：需要「單字破解→組合→本篇推論」實例時讀。

不讀 `FULL-SITE-HANDOFF.md`、App 實作 skill、舊 TTS handoff 或其他篇教材，除非任務真的跨入那些範圍。

### B. 已核准教材轉入正式 App

固定讀：

1. `FULL-SITE-HANDOFF.md`。
2. 本檔。
3. `.claude/skills/implement-guwen-app/SKILL.md`。
4. 完整 active 教材主檔及其核准邊界。
5. 現行相關程式碼、型別與測試。

按需讀 App skill 指定的實作契約、schema mapping、互動或 TTS reference。除非使用者同時要求修改教材，不讀完整教材設計規範。

### C. 同篇只修改一兩題

同一對話已完成開場讀取且來源未變時，只回讀：

- 主檔狀態與本輪範圍；
- 目標題及必要的相鄰題；
- 全篇正解位置序列；
- 受影響的舊鑰匙；
- 本次真正涉及的格式或設計條款。

不得每次小修都重讀整份 SOP、進度表、鑰匙庫、兩個 skill 或其他篇教材。

### D. 只修排版、預覽或 TTS

讀 App 實作 skill、受影響的 MD／元件、必要的格式或 TTS reference。不要讀完整進度表、鑰匙庫或教材設計 standard。

### E. 維護整體工作流程

讀本檔、兩個 skill、`AGENTS.md`、進度表、鑰匙規則與格式契約。教材主檔只作具體衝突的抽樣證據，不列入固定必讀。

## 3. 重寫與多版本教材

孩子實測後可以重寫已完成或已上線教材。多份教材檔可以同時存在，但必須明確分類：

- `active`：目前唯一可繼續撰寫、核准或實作的版本。
- `archived`：舊版、重寫前版本或歷史上線版，只供追溯。
- `draft-rewrite`：已宣布重寫但尚未取代 active 的候選版。

規則：

1. `GUWEN-PROJECT-STATUS.md` 必須記錄每篇的 active 路徑；不得從檔名、日期或資料夾位置猜測。
2. 宣布重寫時，建立新的核准時間線。舊版核准、鑰匙與提醒次數不得自動倒灌。
3. 重寫版何時升為 active，必須由使用者明確決定。
4. 舊版可以封存，不因存在兩份就刪除；只有確認為無追溯價值的重複副本才刪。
5. Archived 檔不得出現在新對話必讀鏈、App 實作來源或教材 catalog。
6. 同一 active 版本只保留一個固定主檔；日常修訂不得另建「最新版2」。

## 4. 教材撰寫與核准

1. 新內容一律標為草稿或待審。
2. 只有使用者明確核准，才可標為已核准或同步正式 App。
3. 修改已核准內容前，列出目前版、建議版、理由與教學影響，等待核准。
4. 古文全文、真實古文線索、出處、用字與語序不得自行改寫。
5. 每輪只處理約定的小範圍；不要因已讀完整主檔而大量續寫。
6. 教材格式遵守 `GUWEN-MARKDOWN-FORMAT.md`，但格式調整不構成改寫內容的授權。
7. 核准新鑰匙或實際發生舊鑰匙提醒後，才更新鑰匙庫。

## 5. GitHub 成人預覽

教材主檔是唯一資料來源；成人以 `/#/guwen-draft-preview` 審孩子實際看見與聽見的內容。

教材對話完成本輪草稿後，先在 active 主檔標為草稿或待審，通過必要檢查並 push，再交付以下兩種可點連結：

1. GitHub MD：固定分支上的 active 主檔，直接定位到本輪第一個目標題的標題行；多個不相鄰題目才分別列連結。
2. 成人 App 狀態預覽：直接定位到本輪第一個目標題的作答前畫面。

連結格式：

```text
https://github.com/icelog-TU/Justin-Learning-App/blob/claude/chinese-learning-app-justin-yjcfam/<active主檔路徑>#L<該題標題行號>

https://icelog-tu.github.io/Justin-Learning-App/#/guwen-draft-preview?lessonPath=<URL 編碼後的 active 主檔路徑>&questionNumber=<人類題號>&state=answering
```

`lessonPath` 必須直接取自 `GUWEN-PROJECT-STATUS.md` 該篇列所指定的 active 主檔路徑，並作為查詢參數進行 URL 編碼；不得猜測或自行縮寫篇名，也不得再產生舊式 `lesson=<lessonId>` 新連結。例如第三篇使用：

```text
https://icelog-tu.github.io/Justin-Learning-App/#/guwen-draft-preview?lessonPath=03-guwen-kezhouqiujian-decoder-content.md&questionNumber=4&state=answering
```

舊 `lesson` 連結只保留既有相容性。若 `lessonPath` 不存在或拼錯，預覽器必須明確報錯，不得默默改開第一篇。

預覽頁可切換作答前、第一次答錯與答對後。每輪固定提供 `state=answering`；只有本輪修改答錯提示或答對後內容時，才另附相同網址的 `state=wrong` 或 `state=correct`。

交付連結前必須實際開啟一次，確認畫面顯示的篇名、題號及作答狀態都與本輪範圍相同；三者任一不符，不得把該網址交給使用者。

純教材文字修改可更新主檔並 push，預覽器直接讀取固定分支；只有預覽器程式或正式 App 改動才需要完整建置與部署。

預覽時檢查作答前、第一次答錯、答對後、線索出處、逐段／整頁 TTS。出處與成人資料只顯示、不朗讀。

## 6. 發音流程

排版與發音在 GitHub MD 預覽一起審，正式實作後再於孩子 App 複核。

- 不做全文多音字掃描、候選去重、代表句、catalog、實聽台、中央回傳或兩階段結案。
- 舊 audit 狀態不是教材核准、App 實作、commit、push 或部署門檻。
- 實際念錯時，以完整詞語定位，在正常預覽循環內做最窄範圍 TTS 修正並測試鄰近用法。
- 不為修音改動孩子看到的古文原字或真實古文線索。
- 舊 audit 頁面、catalog、Firestore 與 stage 報告只作歷史資料；除非使用者明確要求，不新增或更新。

## 7. 每輪同步與交付

開始修改前：

1. Fetch 固定分支。
2. 確認目標檔遠端最新版與 SHA。
3. 檢查工作樹並保留其他工作。

教材有實質進度時：

1. 更新同一 active 主檔。
2. 更新進度表的當篇列。
3. 只在核准後更新鑰匙資料。
4. 執行任務相關檢查。
5. Commit、push，並從遠端回讀。
6. 審稿回覆只列本輪範圍、2–5 點修改重點、待審／核准狀態、驗證結果與 commit SHA。
7. 同時提供第 5 節規定的 GitHub MD 與成人 App 狀態預覽連結；不在對話重貼整篇或整題。只有使用者明確要求逐字比較時，才貼必要的最小片段。

## 8. 多對話並行與跨篇規則

GitHub 固定分支是不同對話之間唯一的同步層；專案記憶與聊天內容不能代替遠端同步。

### 寫入責任

- 不同篇教材可以並行，但同一 active 主檔同一時間只由一個主要對話修改。
- 教材對話只修改本篇 active 主檔、進度表的本篇列，以及鑰匙庫中本篇已核准區段。
- 同一 App 模組或共用元件同一時間只交由一個實作對話修改。
- SOP、skill、設計標準、格式契約與 App 共通契約只由明確的流程維護任務修改；普通教材對話不得另建副本。

### 每次寫入前後

1. 準備修改前 fetch 固定分支，記錄遠端 HEAD，並與本對話上次使用的 SHA 比較。
2. HEAD 改變時，先查看期間內的變更檔案；只重讀與本任務相關且 blob 已改變的檔案。
3. 同一主檔、共用檔或直接依賴已被其他對話修改時，先整合遠端內容；語意衝突無法安全判定時停止並請使用者決定。
4. Commit 或 push 前再次 fetch；若遠端前進，重新執行上述比對與必要驗證。
5. 禁止 force push、覆蓋遠端主檔或用較舊完整檔案取代他人局部更新。
6. 遠端相關 blob 未改變時，不重讀整份 SOP、skill、設計標準或教材。

### 課程中發現跨篇問題

1. 當篇對話可以在使用者核准後修正本輪題目，但不得順手建立或改寫共用規範。
2. 先整理「問題、建議規則、唯一權威檔案、只適用未來或需回查既有教材、可能受影響範圍」。
3. 將提案交給流程維護任務；同一規則只寫入一個權威位置：
   - 教材難度、題幹、選項、線索與認知負荷：`design-standard.md`。
   - 任務分流、核准、重寫與同步程序：本檔。
   - 最短入口路由：`AGENTS.md`。
   - Markdown 欄位與結構：`GUWEN-MARKDOWN-FORMAT.md`。
   - skill 操作步驟或 App 共通技術契約：對應 skill 或其直接 reference。
4. 其他文件只引用權威位置，不複製詳細規則，也不新增 handoff 或規則變更日誌；歷史由 Git 記錄。
5. 共通規則預設只約束後續新寫或本輪明確範圍。是否回查、重審或批次修改既有已核准教材，必須由使用者另行核准範圍。

## 9. 何時重新讀取

同一對話讀過一次後，只在以下情況重讀：

- 使用者說規則或遠端已更新；
- 遠端 SHA 改變且涉及本次依賴檔；
- 其他對話剛修改同一主檔或共用檔；
- 任務從教材切換到 App，或反向切換；
- 出現矛盾、版本疑慮或上下文已不足；
- 寫入共用檔前需要重新比對最新版。

檔案 blob 未改變時，不因下一則訊息或一兩題小修而全文重讀。
